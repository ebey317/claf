Switch MADAM / SENSEI mode.

Valid commands:
/madam local
/madam hybrid
/madam cloud

Argument received:
$ARGUMENTS

Rules:
- Valid modes are only: local, hybrid, cloud.
- Run this command exactly:

/home/elijah/scripts/set_sensei_mode.sh $ARGUMENTS

After running, verify the health output.
Do not claim success unless active_mode matches $ARGUMENTS.
