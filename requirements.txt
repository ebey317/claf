# Core proxy server
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0
pydantic>=2.0,<3.0

# Voice bridge (optional — only needed when running voice_bridge.py)
# edge-tts>=6.1

# Dev / test
# pytest>=8.0
# pytest-asyncio>=0.23
